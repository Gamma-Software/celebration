import {ConfigDropdown} from "@/components/config-dropdown";


export default function Footer() {
  return (
    <footer className="sticky bottom-0 right-0 z-50 bg-background">
        <ConfigDropdown/>
    </footer>
  )
}
